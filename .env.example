# Template for environment variables — copy this file to .env and fill in values for your machine.
# .env is gitignored; .env.example is committed so contributors know what to configure.

# Path to your local Java installation — required by PySpark to start the Java gateway.
# To find yours: run `/usr/libexec/java_home` in your terminal (macOS).
JAVA_HOME=
